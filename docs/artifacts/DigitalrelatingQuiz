<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Self Assessment for Visualization — Digital Relating Quiz</title>
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;0,400;1,300;1,400&family=DM+Mono:wght@300;400;500&display=swap" rel="stylesheet">
<style>
  :root {
    --bg: #F5F0E8;
    --ink: #1A1410;
    --muted: rgba(26,20,16,0.45);
    --line: rgba(26,20,16,0.1);
    --accent: #8B4513;
    --teal: #0D7A70;
    --c-relational:   #C04878;
    --c-erotic:       #8040A8;
    --c-emotional:    #2878A0;
    --c-neuro:        #3A7030;
    --c-mentalhealth: #906010;
    --c-identity:     #B85020;
    --c-needs:        #107870;
    --c-somatic:      #5830A0;
  }
  * { box-sizing: border-box; margin: 0; padding: 0; }
  body {
    background: var(--bg);
    color: var(--ink);
    font-family: 'DM Mono', monospace;
    font-size: 14px;
    line-height: 1.6;
    min-height: 100vh;
  }

  /* ── SCREENS ── */
  .screen { display: none; }
  .screen.active { display: block; }

  /* ── SHARED ── */
  .container { max-width: 720px; margin: 0 auto; padding: 48px 24px 80px; }
  .serif { font-family: 'Cormorant Garamond', serif; }

  h1 { font-family: 'Cormorant Garamond', serif; font-size: clamp(28px, 5vw, 42px); font-weight: 300; font-style: italic; line-height: 1.15; margin-bottom: 16px; }
  h2 { font-family: 'Cormorant Garamond', serif; font-size: 22px; font-weight: 400; font-style: italic; margin-bottom: 12px; }

  .eyebrow { font-size: 10px; letter-spacing: 0.25em; text-transform: uppercase; color: var(--muted); margin-bottom: 12px; }
  .rule { height: 1px; background: var(--line); margin: 28px 0; }
  .sub { font-size: 12px; color: var(--muted); line-height: 1.75; }

  .btn {
    display: inline-block; cursor: pointer;
    background: var(--ink); color: var(--bg);
    border: none; font-family: 'DM Mono', monospace;
    font-size: 11px; letter-spacing: 0.15em; text-transform: uppercase;
    padding: 14px 32px; border-radius: 2px;
    transition: opacity 0.2s; margin-top: 8px;
  }
  .btn:hover { opacity: 0.8; }
  .btn-outline {
    background: transparent; color: var(--ink);
    border: 1px solid var(--ink);
  }
  .btn-teal { background: var(--teal); }

  /* ── PROGRESS ── */
  .progress-bar {
    height: 3px; background: var(--line);
    border-radius: 2px; margin-bottom: 36px; overflow: hidden;
  }
  .progress-fill {
    height: 100%; background: var(--accent);
    border-radius: 2px; transition: width 0.4s ease;
  }
  .progress-label { font-size: 10px; color: var(--muted); margin-bottom: 8px; letter-spacing: 0.1em; }

  /* ── SECTION HEADER ── */
  .section-header {
    background: rgba(26,20,16,0.04);
    border-left: 3px solid var(--accent);
    padding: 14px 18px; margin-bottom: 28px; border-radius: 0 3px 3px 0;
  }
  .section-header h2 { margin-bottom: 4px; font-size: 18px; }
  .section-header p { font-size: 11px; color: var(--muted); }

  /* ── QUESTION ── */
  .question { margin-bottom: 32px; }
  .q-label { font-size: 13px; line-height: 1.65; margin-bottom: 14px; color: var(--ink); }
  .q-label strong { font-weight: 500; color: var(--accent); font-size: 11px; letter-spacing: 0.08em; display: block; margin-bottom: 4px; }

  /* scale selector */
  .scale {
    display: flex; gap: 0;
    background: rgba(26,20,16,0.04);
    border-radius: 4px; overflow: hidden;
    border: 1px solid var(--line);
  }
  .scale-opt {
    flex: 1; text-align: center; padding: 10px 4px;
    cursor: pointer; font-size: 12px; font-weight: 500;
    color: var(--muted); transition: all 0.15s;
    border-right: 1px solid var(--line);
    user-select: none;
  }
  .scale-opt:last-child { border-right: none; }
  .scale-opt:hover { background: rgba(26,20,16,0.07); color: var(--ink); }
  .scale-opt.selected { background: var(--ink); color: var(--bg); }
  .scale-labels { display: flex; justify-content: space-between; margin-top: 5px; }
  .scale-labels span { font-size: 9px; color: var(--muted); letter-spacing: 0.06em; }

  /* safety notice */
  .safety-notice {
    background: rgba(139,69,19,0.07);
    border: 1px solid rgba(139,69,19,0.2);
    border-radius: 3px; padding: 14px 18px; margin: 20px 0;
    font-size: 11px; color: var(--accent); line-height: 1.75;
  }
  .safety-notice a { color: var(--accent); }

  /* ── NAV BUTTONS ── */
  .nav-row { display: flex; gap: 12px; align-items: center; margin-top: 36px; flex-wrap: wrap; }
  .nav-row .q-count { font-size: 10px; color: var(--muted); margin-left: auto; }

  /* ── RESULTS SCREEN ── */
  .results-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; margin-top: 28px; }
  @media (max-width: 560px) { .results-grid { grid-template-columns: 1fr; } }

  .radar-wrap { grid-column: 1 / -1; display: flex; flex-direction: column; align-items: center; }

  .dim-card {
    background: rgba(26,20,16,0.03);
    border: 1px solid var(--line);
    border-radius: 3px; padding: 16px;
    position: relative; overflow: hidden;
  }
  .dim-card-accent { position: absolute; top: 0; left: 0; width: 3px; height: 100%; }
  .dim-card-label { font-size: 9px; letter-spacing: 0.18em; text-transform: uppercase; margin-bottom: 6px; }
  .dim-card-name { font-family: 'Cormorant Garamond', serif; font-size: 16px; font-weight: 400; margin-bottom: 8px; line-height: 1.2; }
  .dim-card-bar { height: 6px; background: rgba(26,20,16,0.08); border-radius: 3px; margin-bottom: 6px; overflow: hidden; }
  .dim-card-fill { height: 100%; border-radius: 3px; }
  .dim-card-score { font-size: 11px; color: var(--muted); }
  .dim-card-interp { font-size: 11px; color: var(--muted); line-height: 1.65; margin-top: 8px; font-style: italic; border-top: 1px solid var(--line); padding-top: 8px; }

  /* email capture */
  .email-panel {
    background: rgba(13,122,112,0.05);
    border: 1px solid rgba(13,122,112,0.25);
    border-radius: 3px; padding: 24px 28px;
    margin-top: 32px;
  }
  .email-panel h2 { color: var(--teal); margin-bottom: 8px; }
  .email-panel .sub { margin-bottom: 18px; }
  .email-row { display: flex; gap: 10px; flex-wrap: wrap; }
  .email-input {
    flex: 1; min-width: 200px; padding: 12px 14px;
    border: 1px solid rgba(13,122,112,0.3);
    border-radius: 2px; font-family: 'DM Mono', monospace;
    font-size: 12px; background: white; color: var(--ink); outline: none;
  }
  .email-input:focus { border-color: var(--teal); }
  .name-input {
    width: 100%; padding: 12px 14px; margin-bottom: 10px;
    border: 1px solid rgba(13,122,112,0.3);
    border-radius: 2px; font-family: 'DM Mono', monospace;
    font-size: 12px; background: white; color: var(--ink); outline: none;
  }
  .name-input:focus { border-color: var(--teal); }
  .email-note { font-size: 10px; color: var(--muted); margin-top: 10px; line-height: 1.6; }

  /* thank you */
  .thankyou { text-align: center; padding: 48px 24px; }
  .thankyou h1 { margin-bottom: 16px; }
  .thankyou .sub { margin: 0 auto 28px; max-width: 440px; }

  /* ── RADAR SVG ── */
  svg.radar text { font-family: 'DM Mono', monospace; }
</style>
</head>
<body>

<!-- ═══════════════════════════════════════════
     SCREEN 0 — INTRO
═══════════════════════════════════════════ -->
<div id="screen-intro" class="screen active">
  <div class="container">
    <div class="eyebrow">Digital Relating Quiz</div>
    <h1>Self Assessment<br>for Visualization</h1>
    <div class="rule"></div>
    <p class="sub" style="margin-bottom:20px;">
      Thank you for your interest in exploring who you are — and how to communicate all the aspects of you that would impact your sexuality, dating and wellbeing.
    </p>
    <p class="sub" style="margin-bottom:20px;">
      After you complete the survey you'll see a visual representation of your profile across 8 dimensions. You can enter your email to receive it as a shareable image to explore with people in your life.
    </p>
    <div class="safety-notice">
      This is not a comprehensive mental health or neurodivergency assessment. It's helpful if you have some baseline understanding of your mental health before doing this survey.<br><br>
      If you're having any suicidal ideation, please ask for help and make a safety plan. <a href="https://www.mysafetyplan.org/" target="_blank">Here's a sample one →</a><br><br>
      ND traits adapted from <a href="https://www.livedexperienceeducator.com/blog/theneurodiversitysmorgasbord" target="_blank">Sonny Wise</a>. Some categories adapted from the Dimensions of Desire by <a href="https://drlizpowell.com/" target="_blank">Dr. Liz Powell</a>.
    </div>
    <div class="rule"></div>
    <p class="sub" style="margin-bottom: 24px; font-size:11px;">59 questions across 8 dimensions · takes about 10 minutes · all answers are private</p>
    <button class="btn" onclick="startQuiz()">Begin →</button>
  </div>
</div>

<!-- ═══════════════════════════════════════════
     SCREEN 1 — QUIZ
═══════════════════════════════════════════ -->
<div id="screen-quiz" class="screen">
  <div class="container">
    <div class="progress-label" id="progress-label">Section 1 of 8</div>
    <div class="progress-bar"><div class="progress-fill" id="progress-fill" style="width:0%"></div></div>
    <div id="section-content"></div>
    <div class="nav-row">
      <button class="btn btn-outline" id="btn-back" onclick="prevSection()" style="display:none">← Back</button>
      <button class="btn" id="btn-next" onclick="nextSection()">Next →</button>
      <span class="q-count" id="q-count"></span>
    </div>
  </div>
</div>

<!-- ═══════════════════════════════════════════
     SCREEN 2 — RESULTS
═══════════════════════════════════════════ -->
<div id="screen-results" class="screen">
  <div class="container">
    <div class="eyebrow">Your profile</div>
    <h1>Your Visualization</h1>
    <p class="sub" style="margin-bottom:8px;">Each dimension reflects a cluster of your responses. There is no ideal shape — this is a map, not a score.</p>
    <div class="rule"></div>

    <!-- RADAR -->
    <div class="radar-wrap" style="margin-bottom:32px;">
      <svg class="radar" id="radar-svg" viewBox="0 0 440 440" width="100%" style="max-width:440px;"></svg>
    </div>

    <!-- DIMENSION CARDS -->
    <div class="results-grid" id="results-cards"></div>

    <!-- EMAIL CAPTURE -->
    <div class="email-panel">
      <h2>Receive your results</h2>
      <p class="sub">Enter your name and email to receive your visualization as a shareable image, plus a guide to sharing it with people in your life.</p>
      <input class="name-input" id="result-name" type="text" placeholder="Your first name"/>
      <div class="email-row">
        <input class="email-input" id="result-email" type="email" placeholder="your@email.com"/>
        <button class="btn btn-teal" onclick="submitEmail()">Send my results</button>
      </div>
      <p class="email-note">
        Your data is private and will only be used to send you your results. No spam.<br>
        Want to explore this with a partner, metamour, or someone new? Share the quiz link with them — then compare your visualizations.
      </p>
    </div>

    <div class="rule"></div>
    <button class="btn btn-outline" onclick="retakeQuiz()" style="font-size:10px;">Start over</button>
  </div>
</div>

<!-- ═══════════════════════════════════════════
     SCREEN 3 — THANK YOU
═══════════════════════════════════════════ -->
<div id="screen-thankyou" class="screen">
  <div class="container">
    <div class="thankyou">
      <div class="eyebrow">Thank you</div>
      <h1>On its way to you</h1>
      <p class="sub">Your visualization is being put together and will arrive in your inbox shortly. Check your spam folder if you don't see it within a few minutes.</p>
      <div class="rule"></div>
      <p class="sub" style="margin-bottom:24px;">
        Want to go deeper? You can explore how your profile overlaps with someone else's using the full relationship ecosystem mapper — or book a session to walk through your results.
      </p>
      <a href="/" class="btn">Back to site</a>
    </div>
  </div>
</div>

<script>
// ═══════════════════════════════════════════
// DATA
// ═══════════════════════════════════════════

const SECTIONS = [
  {
    id: "needs",
    label: "Unmet Basic Needs",
    color: "#107870",
    desc: "Community, self-regulation, basic needs, education",
    accent: "rgba(16,120,112,0.12)",
    questions: [
      { n:1,  text: "How big is your need / gaps in community access and support?", lo: "no gaps", hi: "significant gaps" },
      { n:2,  text: "How difficult is self-regulation for you currently?", lo: "very easy", hi: "very difficult" },
      { n:3,  text: "How big are your gaps in basic needs (food, housing, etc)?", lo: "needs met", hi: "significant gaps" },
      { n:4,  text: "How big is your need for more sex and relationship education / increased sexual self-awareness?", lo: "none", hi: "significant need" },
      { n:5,  text: "How difficult is it for you to set boundaries?", lo: "easy", hi: "very difficult" },
      { n:6,  text: "How strong are your avoidance patterns (in conflict, relationships, processing)?", lo: "minimal", hi: "strong" },
    ]
  },
  {
    id: "mentalhealth",
    label: "Mental Health Load",
    color: "#906010",
    desc: "Current intensity of anxiety, depression, dissociation, and other symptoms",
    accent: "rgba(144,96,16,0.1)",
    safety: true,
    questions: [
      { n:7,  text: "How strong / frequent are your intrusive thoughts?", lo: "rare", hi: "constant" },
      { n:8,  text: "How strong / frequent is your hypervigilance / elevated nervous system?", lo: "rare", hi: "constant" },
      { n:9,  text: "How strong / frequent are your dissociation / shutdown responses?", lo: "rare", hi: "frequent" },
      { n:12, text: "How important / present is substance use in your daily life right now?", lo: "not present", hi: "significant" },
      { n:13, text: "How frequent / intense is your suicidal ideation lately?", lo: "none", hi: "frequent", safety: true },
      { n:14, text: "How intense / impactful are your depression symptoms currently?", lo: "minimal", hi: "intense" },
      { n:15, text: "How intense / impactful are your anxiety symptoms currently?", lo: "minimal", hi: "intense" },
      { n:16, text: "How intense / impactful are your OCD symptoms currently?", lo: "minimal", hi: "intense" },
      { n:17, text: "How intense / impactful are your psychotic symptoms currently?", lo: "none", hi: "significant" },
    ]
  },
  {
    id: "identity",
    label: "Identity & Body Stressors",
    color: "#B85020",
    desc: "Gender, trauma, somatic awareness, disabilities",
    accent: "rgba(184,80,32,0.08)",
    questions: [
      { n:10, text: "How intense is your identity-related trauma (dysphoria, race, gender, body image, etc)?", lo: "minimal", hi: "intense" },
      { n:11, text: "How intense is your religious or culturally-impacted trauma?", lo: "minimal", hi: "intense" },
      { n:40, text: "How connected are you to your somatic / body awareness (pain, movement, shutdown)?", lo: "disconnected", hi: "very connected" },
      { n:41, text: "How connected are you to your breath and its capacity to regulate / enhance pleasure?", lo: "disconnected", hi: "very connected" },
      { n:42, text: "Are there physical health or other disabilities which impact play / relationships?", lo: "none", hi: "significant impact" },
      { n:43, text: "How expansive is your gender / gender identity? (cis = 0, expansive = 5)", lo: "cis", hi: "expansive" },
    ]
  },
  {
    id: "erotic",
    label: "Erotic & Sensual Importance",
    color: "#8040A8",
    desc: "Desire, kink, power, sensuality, frequency",
    accent: "rgba(128,64,168,0.08)",
    questions: [
      { n:18, text: "How important is the presence of power and control dynamics for your play / relationships?", lo: "not important", hi: "essential" },
      { n:19, text: "How important is being the initiator / giver / top for your play / relationships?", lo: "not important", hi: "essential" },
      { n:20, text: "How important is the use of pain for your play / relationships?", lo: "not important", hi: "essential" },
      { n:21, text: "How important are kink dynamics, experiences, and frameworks for your play / relationships?", lo: "not important", hi: "essential" },
      { n:22, text: "How important is intensity in kink (pain / power / edge play) for your play / relationships?", lo: "not important", hi: "essential" },
      { n:23, text: "How important is fetish exploration for your play / relationships?", lo: "not important", hi: "essential" },
      { n:30, text: "How comfortable are you with sexual / sensual / kink exploration with yourself?", lo: "not comfortable", hi: "very comfortable" },
      { n:31, text: "How comfortable are you with sexual / sensual / kink exploration with others?", lo: "not comfortable", hi: "very comfortable" },
      { n:32, text: "How important is sensual touch and exploration for your play?", lo: "not important", hi: "essential" },
      { n:34, text: "At what frequency do you prefer sex?", lo: "rarely", hi: "daily or more" },
      { n:35, text: "How important is variety and exploring taboos in your play?", lo: "not important", hi: "essential" },
      { n:36, text: "How important is erotic exploration for your play?", lo: "not important", hi: "essential" },
    ]
  },
  {
    id: "relational",
    label: "Relational Style Flexibility",
    color: "#C04878",
    desc: "Openness to entanglement, multiple connections, commitment style",
    accent: "rgba(192,72,120,0.07)",
    questions: [
      { n:24, text: "How strong is your need for play to happen only in a committed / long-term relationship?", lo: "no need", hi: "strong need", invert: true },
      { n:25, text: "How open are you to new play situations and experiences, or play with strangers who may become friends?", lo: "not open", hi: "very open" },
      { n:26, text: "How open are you to play at parties or festivals?", lo: "not open", hi: "very open" },
      { n:29, text: "How important is escalating / intertwining your lives in your relationships?", lo: "not important", hi: "very important" },
      { n:37, text: "How important is the gender identity of a possible partner when deciding to play?", lo: "not important", hi: "very important", invert: true },
      { n:38, text: "How open are you to multiple partners in any format (threesomes, play at parties, multiple longer-term connections)?", lo: "not open", hi: "very open" },
      { n:39, text: "How important are romantic experiences for your play / relationships?", lo: "not important", hi: "very important" },
    ]
  },
  {
    id: "emotional",
    label: "Emotional Nurturance",
    color: "#2878A0",
    desc: "Need for co-regulation, emotional processing, calming",
    accent: "rgba(40,120,160,0.07)",
    questions: [
      { n:27, text: "How important is emotional nurturance and processing for your kink / sexual relationships?", lo: "not important", hi: "essential" },
      { n:28, text: "How important are calming / co-regulation experiences for your kink / sexual relationships?", lo: "not important", hi: "essential" },
    ]
  },
  {
    id: "somatic",
    label: "Somatic & Spiritual Importance",
    color: "#5830A0",
    desc: "Spirituality, breathwork, body-based awareness in play",
    accent: "rgba(88,48,160,0.07)",
    questions: [
      { n:33, text: "How important is spirituality / sacredness and ritual for your play?", lo: "not important", hi: "essential" },
      { n:40, text: "How connected are you to your somatic / body awareness in play and relationships?", lo: "disconnected", hi: "deeply connected" },
      { n:41, text: "How connected are you to your breath and its capacity to regulate / enhance pleasure?", lo: "disconnected", hi: "deeply connected" },
    ]
  },
  {
    id: "neurodivergence",
    label: "Neurodivergence",
    color: "#3A7030",
    desc: "ND traits — adapted from Sonny Wise's Neurodiversity Smorgasbord",
    accent: "rgba(58,112,48,0.07)",
    intro: "These questions are about traits you may experience — not deficits. There is no wrong shape here.",
    questions: [
      { n:44, text: "Altered states (lucid dreaming, daydreaming, hallucinations, visions, mania, psychosis, dissociation)", lo: "rarely", hi: "frequently" },
      { n:45, text: "Differences in attention (regulating attention, monotropism, flow state, hyperfocusing)", lo: "rarely", hi: "frequently" },
      { n:46, text: "Communication differences (non-spoken communication, echolalia, differences in tone, pace, eye contact)", lo: "rarely", hi: "frequently" },
      { n:47, text: "Eating differences or challenges", lo: "rarely", hi: "frequently" },
      { n:48, text: "Emotional processing differences (differences in intensity, duration, naming, describing emotions)", lo: "rarely", hi: "frequently" },
      { n:49, text: "Empathy differences (cognitive, emotional, somatic, mirror, aesthetic empathy)", lo: "rarely", hi: "frequently" },
      { n:50, text: "Memory differences (working memory, semantic, prospective, episodic, procedural memory)", lo: "rarely", hi: "frequently" },
      { n:51, text: "Motor functioning differences (motor control, fine motor, gross motor, motor tics)", lo: "rarely", hi: "frequently" },
      { n:52, text: "Plurality (multiplicity, distinct multiple selves, inner child, multiple inner dialogues, parts)", lo: "rarely", hi: "frequently" },
      { n:53, text: "Sensory differences (sensory seeking, avoiding, hypersensitivity, hyposensitivity, synesthesia)", lo: "rarely", hi: "frequently" },
      { n:54, text: "Sleep differences or problems", lo: "rarely", hi: "frequently" },
      { n:55, text: "Stimming (auditory, tactile, vocal, visual, vestibular, proprioceptive, olfactory stims)", lo: "rarely", hi: "frequently" },
      { n:56, text: "Time perception differences (variability, speeding up, slowing down, cyclical vs linear time)", lo: "rarely", hi: "frequently" },
      { n:57, text: "Visual imagination differences (aphantasia, partial aphantasia, hyperphantasia)", lo: "rarely", hi: "frequently" },
      { n:58, text: "Voice hearing (internal monologue, intrusive thoughts, external or internal voices)", lo: "rarely", hi: "frequently" },
      { n:59, text: "Differences in processing or expressing information", lo: "rarely", hi: "frequently" },
    ]
  }
];

// ═══════════════════════════════════════════
// STATE
// ═══════════════════════════════════════════
let currentSection = 0;
let answers = {}; // { qN: 0-5 }
let scores = {};  // { dimensionId: 0-1 }

// ═══════════════════════════════════════════
// QUIZ LOGIC
// ═══════════════════════════════════════════
function startQuiz() {
  showScreen('quiz');
  renderSection();
}

function renderSection() {
  const sec = SECTIONS[currentSection];
  const totalQ = SECTIONS.reduce((a,s) => a + s.questions.length, 0);
  const doneQ = SECTIONS.slice(0, currentSection).reduce((a,s) => a + s.questions.length, 0);
  const pct = Math.round((currentSection / SECTIONS.length) * 100);

  document.getElementById('progress-fill').style.width = pct + '%';
  document.getElementById('progress-label').textContent = `Section ${currentSection+1} of ${SECTIONS.length} — ${sec.label}`;
  document.getElementById('q-count').textContent = `Q${doneQ+1}–${doneQ+sec.questions.length} of ${totalQ}`;
  document.getElementById('btn-back').style.display = currentSection > 0 ? 'inline-block' : 'none';
  document.getElementById('btn-next').textContent = currentSection === SECTIONS.length - 1 ? 'See my results →' : 'Next →';

  let html = `
    <div class="section-header" style="border-left-color:${sec.color}; background:${sec.accent}">
      <h2 style="color:${sec.color}">${sec.label}</h2>
      <p>${sec.desc}</p>
    </div>
  `;

  if (sec.intro) {
    html += `<p class="sub" style="margin-bottom:24px; font-style:italic;">${sec.intro}</p>`;
  }

  if (sec.safety) {
    html += `<div class="safety-notice">
      If you're experiencing suicidal ideation, please reach out. <a href="https://www.mysafetyplan.org/" target="_blank">Safety planning resource →</a> &nbsp;|&nbsp; 
      Crisis line: <strong>988</strong> (US) or text HOME to 741741
    </div>`;
  }

  sec.questions.forEach((q, i) => {
    const val = answers[q.n] !== undefined ? answers[q.n] : null;
    html += `
      <div class="question">
        <div class="q-label">
          <strong>Q${q.n}</strong>
          ${q.text}
        </div>
        <div class="scale">
          ${[0,1,2,3,4,5].map(v => `
            <div class="scale-opt ${val===v?'selected':''}" 
              onclick="selectAnswer(${q.n}, ${v}, this)"
              data-q="${q.n}" data-v="${v}">${v}</div>
          `).join('')}
        </div>
        <div class="scale-labels">
          <span>0 — ${q.lo}</span>
          <span>${q.hi} — 5</span>
        </div>
      </div>
    `;
  });

  document.getElementById('section-content').innerHTML = html;
  window.scrollTo(0, 0);
}

function selectAnswer(qn, val, el) {
  answers[qn] = val;
  // update UI
  document.querySelectorAll(`[data-q="${qn}"]`).forEach(opt => {
    opt.classList.toggle('selected', parseInt(opt.dataset.v) === val);
  });
}

function nextSection() {
  // check all answered in this section
  const sec = SECTIONS[currentSection];
  const missing = sec.questions.filter(q => answers[q.n] === undefined);
  if (missing.length > 0) {
    // highlight unanswered — scroll to first
    const firstQ = missing[0].n;
    const el = document.querySelector(`[data-q="${firstQ}"]`);
    if (el) {
      el.closest('.question').scrollIntoView({ behavior: 'smooth', block: 'center' });
      el.closest('.question').style.outline = '2px solid #B85020';
      setTimeout(() => el.closest('.question').style.outline = '', 2000);
    }
    return;
  }

  if (currentSection < SECTIONS.length - 1) {
    currentSection++;
    renderSection();
  } else {
    computeScores();
    showResults();
  }
}

function prevSection() {
  if (currentSection > 0) {
    currentSection--;
    renderSection();
  }
}

function computeScores() {
  scores = {};
  SECTIONS.forEach(sec => {
    const vals = sec.questions.map(q => {
      let v = (answers[q.n] || 0) / 5;
      if (q.invert) v = 1 - v;
      return v;
    });
    scores[sec.id] = vals.reduce((a,b) => a+b, 0) / vals.length;
  });
}

// ═══════════════════════════════════════════
// RESULTS
// ═══════════════════════════════════════════

const INTERPRETATIONS = {
  needs: [
    [0, 0.3, "Your basic needs and community feel relatively met right now."],
    [0.3, 0.6, "Some gaps in community, resources, or self-regulation are present and worth naming."],
    [0.6, 1, "Significant unmet needs — this is structural, not personal. These gaps shape everything else."],
  ],
  mentalhealth: [
    [0, 0.3, "Your mental health load is relatively low right now."],
    [0.3, 0.6, "A moderate mental health load is present — worth considering in relational and sexual contexts."],
    [0.6, 1, "High mental health load. This shapes how you connect, play, and need to be met. Important context for any relationship."],
  ],
  identity: [
    [0, 0.3, "Identity and body stressors are relatively low, and somatic connection is present."],
    [0.3, 0.6, "Some identity or body-related stress, and a developing somatic awareness."],
    [0.6, 1, "Significant identity stressors and/or strong somatic awareness — both matter in relational and sexual contexts."],
  ],
  erotic: [
    [0, 0.3, "Erotic and sensual dimensions are less central to your relational world right now."],
    [0.3, 0.6, "Moderate erotic and sensual importance — some dimensions matter more than others."],
    [0.6, 1, "High erotic and sensual importance — these dimensions are central to how you connect and need to be met."],
  ],
  relational: [
    [0, 0.3, "You prefer more defined, contained relational structures — commitment and clarity matter."],
    [0.3, 0.6, "Moderate flexibility — some openness to different relational forms, some preference for stability."],
    [0.6, 1, "High relational flexibility — openness to multiple connections, new situations, and varied forms of relating."],
  ],
  emotional: [
    [0, 0.3, "Emotional processing and co-regulation are less central to your relational needs right now."],
    [0.3, 0.6, "Emotional nurturance matters moderately — context-dependent."],
    [0.6, 1, "Emotional nurturance and co-regulation are central to how you relate and need to be held."],
  ],
  somatic: [
    [0, 0.3, "Somatic and spiritual dimensions are less central to your relational world."],
    [0.3, 0.6, "Some somatic or spiritual importance — these dimensions are present but not defining."],
    [0.6, 1, "Somatic and spiritual connection are important to how you experience play and relationship."],
  ],
  neurodivergence: [
    [0, 0.3, "ND traits are less present or prominent in your current experience."],
    [0.3, 0.6, "A moderate presence of ND traits — these shape how you process, communicate, and connect."],
    [0.6, 1, "ND traits are significantly present — these are part of your operating system, not a deficit. They shape everything and need to be named with partners."],
  ],
};

function getInterp(id, score) {
  const ranges = INTERPRETATIONS[id];
  for (const [lo, hi, text] of ranges) {
    if (score >= lo && score <= hi) return text;
  }
  return ranges[ranges.length-1][2];
}

function showResults() {
  // Build radar
  const svg = document.getElementById('radar-svg');
  const cx = 220, cy = 220, r = 160;
  const n = SECTIONS.length;
  const angles = SECTIONS.map((_, i) => (i / n) * Math.PI * 2 - Math.PI / 2);
  const getPoint = (a, v) => ({ x: cx + Math.cos(a) * r * v, y: cy + Math.sin(a) * r * v });

  let svgHtml = '';

  // grid rings
  [0.25, 0.5, 0.75, 1].forEach(lv => {
    const pts = angles.map(a => getPoint(a, lv));
    const path = pts.map((p,i) => `${i===0?'M':'L'}${p.x.toFixed(1)},${p.y.toFixed(1)}`).join(' ') + ' Z';
    svgHtml += `<path d="${path}" fill="none" stroke="rgba(26,20,16,0.08)" stroke-width="1"/>`;
  });

  // spokes
  angles.forEach((a, i) => {
    const e = getPoint(a, 1);
    svgHtml += `<line x1="${cx}" y1="${cy}" x2="${e.x.toFixed(1)}" y2="${e.y.toFixed(1)}" stroke="rgba(26,20,16,0.08)" stroke-width="1"/>`;
  });

  // data shape
  const dataPoints = SECTIONS.map((sec, i) => getPoint(angles[i], scores[sec.id] || 0));
  const dataPath = dataPoints.map((p,i) => `${i===0?'M':'L'}${p.x.toFixed(1)},${p.y.toFixed(1)}`).join(' ') + ' Z';
  svgHtml += `<path d="${dataPath}" fill="rgba(139,69,19,0.12)" stroke="rgba(139,69,19,0.7)" stroke-width="2"/>`;

  // dimension dots + labels
  SECTIONS.forEach((sec, i) => {
    const score = scores[sec.id] || 0;
    const pt = getPoint(angles[i], score);
    const lp = getPoint(angles[i], 1.22);
    // dot
    svgHtml += `<circle cx="${pt.x.toFixed(1)}" cy="${pt.y.toFixed(1)}" r="5" fill="${sec.color}"/>`;
    // label — first word only for radar legibility
    const words = sec.label.split(' ');
    const short = words.length > 2 ? words[0] + ' ' + words[1] : words[0];
    const anchor = Math.cos(angles[i]) > 0.2 ? 'start' : Math.cos(angles[i]) < -0.2 ? 'end' : 'middle';
    svgHtml += `<text x="${lp.x.toFixed(1)}" y="${lp.y.toFixed(1)}" text-anchor="${anchor}" dominant-baseline="middle" font-size="9" fill="${sec.color}" font-family="'DM Mono',monospace">${short}</text>`;
  });

  svg.innerHTML = svgHtml;

  // Build dimension cards
  let cardsHtml = '';
  SECTIONS.forEach(sec => {
    const score = scores[sec.id] || 0;
    const pct = Math.round(score * 100);
    const interp = getInterp(sec.id, score);
    cardsHtml += `
      <div class="dim-card">
        <div class="dim-card-accent" style="background:${sec.color}"></div>
        <div class="dim-card-label" style="color:${sec.color}">${sec.label}</div>
        <div class="dim-card-bar"><div class="dim-card-fill" style="width:${pct}%;background:${sec.color}"></div></div>
        <div class="dim-card-score">${(score * 5).toFixed(1)} / 5</div>
        <div class="dim-card-interp">${interp}</div>
      </div>
    `;
  });
  document.getElementById('results-cards').innerHTML = cardsHtml;

  showScreen('results');
  window.scrollTo(0, 0);
}

// ═══════════════════════════════════════════
// EMAIL SUBMIT
// ═══════════════════════════════════════════
function submitEmail() {
  const email = document.getElementById('result-email').value.trim();
  const name = document.getElementById('result-name').value.trim();
  if (!email || !email.includes('@')) {
    document.getElementById('result-email').focus();
    return;
  }

  // Build scores summary for email/webhook
  const scoresSummary = SECTIONS.map(sec => ({
    dimension: sec.label,
    score: (scores[sec.id] * 5).toFixed(1),
    interpretation: getInterp(sec.id, scores[sec.id])
  }));

  // ── SEND TO YOUR ENDPOINT ──
  // Replace this URL with your Make/Zapier webhook or email service
  // The payload includes name, email, and all dimension scores
  const payload = {
    name: name || 'friend',
    email: email,
    scores: scoresSummary,
    raw_scores: scores,
    timestamp: new Date().toISOString()
  };

  // Send to webhook (replace URL with yours)
  const WEBHOOK_URL = 'https://hook.us1.make.com/YOUR_WEBHOOK_ID_HERE';

  fetch(WEBHOOK_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(payload)
  }).catch(() => {
    // Fail silently — still show thank you
  });

  // Also send to Google Sheets via Apps Script if configured
  // Uncomment and replace URL to enable:
  // const SHEETS_URL = 'https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec';
  // fetch(SHEETS_URL, { method:'POST', body: JSON.stringify(payload) }).catch(()=>{});

  showScreen('thankyou');
  window.scrollTo(0, 0);
}

// ═══════════════════════════════════════════
// UTILS
// ═══════════════════════════════════════════
function showScreen(id) {
  document.querySelectorAll('.screen').forEach(s => s.classList.remove('active'));
  document.getElementById('screen-' + id).classList.add('active');
}

function retakeQuiz() {
  answers = {};
  scores = {};
  currentSection = 0;
  showScreen('quiz');
  renderSection();
}
</script>
</body>
</html>
